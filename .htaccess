Options -Indexes
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set Referrer-Policy "same-origin"
</IfModule>
<FilesMatch "^(\.env|\.git|database_setup\.sql)$">
    Require all denied
</FilesMatch>

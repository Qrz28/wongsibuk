APP_ENV=production
APP_TIMEZONE=Asia/Jakarta
DB_HOST=localhost
DB_USER=app_user
DB_PASS=change_me
DB_NAME=wongsibuk
# Comma-separated browser origins; leave empty when the frontend and API share an origin.
ALLOWED_ORIGINS=https://example.com
